/**
 * Training Index
 * 
 * Exports all training components for easy access
 */

require('');
require('');
require('');

module.exports = {
  trainingService,
  trainingController,
  trainingRoutes
};
