/**
 * Simple Test
 * 
 * A simple test file to verify basic Jest functionality
 */

describe('Simple Test', () => {
  it('should pass a basic assertion', () => {
    expect(true).toBe(true);
  });

  it('should add numbers correctly', () => {
    expect(1 + 1).toBe(2);
  });
});
