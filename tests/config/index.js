/**
 * Test Configuration
 * 
 * Exports test configuration for use in tests
 */

// Import the test config
const testConfig = require('../unit/setup/test-config');

// Export the test config
module.exports = testConfig;
